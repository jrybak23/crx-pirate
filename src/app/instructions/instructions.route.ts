import {InstructionsComponent} from './instructions.component';
import {Route} from '@angular/router';

export const instructionRoute: Route = {path: 'instructions', component: InstructionsComponent, pathMatch: 'full'};

